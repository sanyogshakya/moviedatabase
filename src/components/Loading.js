import React from 'react';

const Loading = () => {
  return (
    <h2
      style={{
        display: 'flex',
        justifyContent: 'center',
        height: '70vh',
        alignItems: 'center',
      }}
    >
      Loading...
    </h2>
  );
};

export default Loading;
